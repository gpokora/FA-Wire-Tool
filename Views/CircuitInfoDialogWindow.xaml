<Window x:Class="FireAlarmCircuitAnalysis.Views.CircuitInfoDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Circuit Information" 
        Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Edit Circuit Information" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Circuit ID -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Circuit ID:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Name="txtCircuitID" Margin="10,0,0,0" Height="25"/>
        </Grid>
        
        <!-- Description -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Description:" VerticalAlignment="Top" Margin="0,5,0,0"/>
            <TextBox Grid.Column="1" Name="txtDescription" Margin="10,0,0,0" 
                     Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AcceptsReturn="True"/>
        </Grid>
        
        <!-- Info -->
        <TextBlock Grid.Row="3" Text="Enter the circuit ID and optional description for this circuit configuration." 
                   Foreground="Gray" FontSize="11" Margin="0,0,0,15"/>
        
        <!-- Spacer -->
        <Border Grid.Row="4"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnOK" Content="OK" Width="80" Height="30" Margin="0,0,10,0" 
                    Click="BtnOK_Click" IsDefault="True"/>
            <Button Name="btnCancel" Content="Cancel" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>