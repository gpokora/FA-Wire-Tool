<Window x:Class="FireAlarmCircuitAnalysis.Views.SaveCircuitDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Circuit Configuration" 
        Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Save Circuit Configuration" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <!-- Circuit Name -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Circuit Name:" VerticalAlignment="Center" Width="100"/>
            <TextBox Grid.Column="1" Name="txtCircuitName" Margin="5,0,0,0"/>
        </Grid>
        
        <!-- Description -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Description:" VerticalAlignment="Top" Width="100" Margin="0,5,0,0"/>
            <TextBox Grid.Column="1" Name="txtDescription" Margin="5,0,0,0" 
                     Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AcceptsReturn="True"/>
        </Grid>
        
        <!-- Info -->
        <TextBlock Grid.Row="3" Name="lblInfo" Text="" Foreground="Gray" FontSize="11" Margin="0,0,0,15"/>
        
        <!-- Preview -->
        <GroupBox Grid.Row="4" Header="Circuit Summary" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="10">
                    <TextBlock Name="lblDeviceCount" Text="Devices: 0" Margin="0,2"/>
                    <TextBlock Name="lblMainCount" Text="Main Circuit: 0" Margin="0,2"/>
                    <TextBlock Name="lblBranchCount" Text="T-Taps: 0" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Name="lblTotalLoad" Text="Total Load: 0.000 A" Margin="0,2"/>
                    <TextBlock Name="lblTotalLength" Text="Wire Length: 0.0 ft" Margin="0,2"/>
                    <TextBlock Name="lblStatus" Text="Status: Valid" Margin="0,2"/>
                </StackPanel>
            </Grid>
        </GroupBox>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnSave" Content="Save" Width="80" Height="30" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Name="btnCancel" Content="Cancel" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>